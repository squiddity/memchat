# Plans archive

> **Historical/non-product record:** Every file in `docs/plans/` is a design, evaluation decision, or migration handoff preserved for traceability. Plans are not runtime authority, an installation contract, or a promise that their commands, paths, names, or implementation references still exist.

Use the active product sources for current behavior:

- [`README.md`](../../README.md) and [`AGENTS.md`](../../AGENTS.md) for repository guidance;
- [`skills/mem-import/SKILL.md`](../../skills/mem-import/SKILL.md) and its references for the production mem-import workflow;
- [`docs/smoke-tests.md`](../smoke-tests.md) for validation commands.

Historical plans may retain retired vocabulary and links to deleted implementation files when those details are necessary to explain an earlier decision. The [legacy cleanup feature handoff](2026-08-04-mem-import-legacy-cleanup-feature-handoff.md) is the migration record for retiring those surfaces; it does not restore them as product APIs. The weekly consolidation and efficiency/parity plan below are likewise historical decision records: their cost A/B comparison is waived, no new legacy run is required, and the feature handoff—not parity with the retired runner—is the cleanup input.
